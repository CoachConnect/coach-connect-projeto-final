import styled from "styled-components";

const StyledMainRegisterPage = styled.main`
    margin: 0 auto;
    width: 95%;

    h2{
        color: var(--colorPrimary);
        text-align: center;
    }
`

export  {StyledMainRegisterPage }